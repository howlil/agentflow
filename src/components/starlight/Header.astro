---
import DefaultHeader from '@astrojs/starlight/components/Header.astro';
import LandingHeader from '../landing/LandingHeader.astro';

const isHome = Astro.url.pathname === '/';
---

{isHome ? <LandingHeader /> : <DefaultHeader />}
